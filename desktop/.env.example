# Optional. Place a real .env next to PNRC-OCR.exe to override the default API endpoint.
# If no .env is present, the app falls back to the hardcoded Modal URL in app.py.
PNRC_API_BASE=https://jatin-veritos--pnrc-main-ocr-fastapi-app.modal.run
